# All dependencies now live in the root requirements.txt so a single
# conda env can run both the dashboard and this local ML service.
# This file re-exports them for anyone pointing pip at idn-tts/ directly.
-r ../requirements.txt
